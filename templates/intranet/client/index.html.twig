{% extends 'base.html.twig' %}

{% block title %}Administration clients{% endblock %}

{% block main_page %}

    {% import 'macros/bouton.html.twig' as control %}
    {% import "macros/textAjax.html.twig" as ajax %}

    <div class="container">

        <div class="row">

            <form method="post" action="{{ path('show_client') }}">
                {{ ajax.text('Recherche', 'recherche', path('ajax_search_clients'), term, currentPage) }}
            </form>
        </div>

        <div id="render_list">
        </div>

    </div>
    <script>
        {{ ajax.script(path('ajax_search_clients'), "'" ~ term ~ "'", currentPage) }}
    </script>
{% endblock %}
