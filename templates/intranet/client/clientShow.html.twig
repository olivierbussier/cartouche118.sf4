<img src="{{ asset('/ico/pencil-square.svg') }}"
     data-url="{{ url('editClient', { id: client.id }) }}"
     data-receiver="#client_{{ client.id }}"
     onclick="cEdit(this)">
Full : [{{ client.fullName }}] - Nom : [{{ client.nom }}] - Prénom : [{{ client.prenom }}]
